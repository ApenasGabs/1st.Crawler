{
  "name": "1st.crawler",
  "version": "1.0.0",
  "description": "Just a simple crawler in NodeJS",
  "main": "index.js",
  "dependencies": {
    "axios": "^0.21.4",
    "cheerio": "^1.0.0-rc.10",
    "request": "^2.88.2"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
